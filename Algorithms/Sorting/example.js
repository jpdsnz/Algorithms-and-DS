//------ Sorting ------//

//Javascript built in sort example
/*
const letters = ['a', 'd', 'z', 'e', 'r', 'b'];
const basket = [2, 65, 34, 2, 1, 7, 8];

console.log(letters.sort());
console.log(basket.sort()); //This sort is wrong because the numbers are converted into unicode before sorting!

console.log(basket.sort(function(a,b){
  return a - b;
}));
*/
